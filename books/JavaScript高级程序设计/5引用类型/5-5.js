/**
 * Created by 懒牛牛 on 2017/2/17.
 */

let publicFunc = require('../public');
/*
基本包装类型
引用类型与基本包装类型区别 对象的生存期  基本类型创建的对象是创建就销毁  引用类型创建的对象是执行流离开当前作用域
 */

/*
Boolean类型
重写了valueOf() toString() toLocaleString()
 */

/*
Number类型
重写了valueOf() toString() toLocaleString()
toFixed() 指定小数点位数
toExponential() 指数
toPrecision() 最佳方式
 */

/*
String类型
toString() toLocaleString() valueOf()
length

字符方法
charAt() charCodeAt() 传入基于0的字符位置数字，第一个返回字符，第二个返回字符编码
通过[]也可以实现charAt()的功能

字符串操作方法
concat() 实战中用+
slice()
substr()
substring() （负数参数为0）。对原字符串无影响

字符串位置方法
indexOf() lastIndexOf() 第二个参数表示从哪开始

trim() 删除前置后缀空格
trimLeft()
trimRight()
 */

/*
字符串大小写转换方法
toLowerCase() toLocaleLowerCase() toUpperCase() toLocaleUpperCase()
 */

/*
字符串的模式匹配方法
match()
search()
replace()
split()分割多个字符串
 */

/*
localeCompare() 比较两个字符串首字符在字母表的顺序
 */

/*
fromCharCode() 接受字符编码构造为字符串
 */

/*
html方法(不推荐使用)
 */












