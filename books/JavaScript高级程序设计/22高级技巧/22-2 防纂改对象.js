/**
 * Created by 懒牛牛 on 2017/2/22.
 */

/*
防篡改对象

手工设个每个属性的Configurable Writable Enumerable Value Get Set
还添加了几个方法来指定对象行为
 */

/*
不可扩展对象（不增）
Object.preventExtensions()
 */

/*
密封的对象(不删不增)
Object.seal()
 */

/*
冻结的对象
Object.freeze()不增不删不改
 */















