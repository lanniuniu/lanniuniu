<script>
    /*
    导入 Vue.js 和组件，进行测试

    */

    /*
    编写可被测试的组件

很多组件的渲染输出由它的 props 决定。事实上，如果一个组件的渲染输出完全取决于它的 props，
那么它会让测试变得简单，就好像断言不同参数的纯函数的返回值。
     */

    /*
    断言异步更新

由于 Vue 进行 异步更新 DOM 的情况，一些依赖 DOM 更新结果的断言必须在 Vue.nextTick 回调中进行：
     */

    /*
    期待vue正在做的通用测试集，让不同策略的渲染输出，以便推广
     */
</script>