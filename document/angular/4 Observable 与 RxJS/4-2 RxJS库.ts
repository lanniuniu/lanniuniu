/*
创建可观察对象的函数
import { ajax } from 'rxjs/ajax';
import { fromEvent } from 'rxjs';
import { interval } from 'rxjs';
import { fromPromise } from 'rxjs';
 */

/*
管道操作中可以有很多操作符(比如 from of map filter),可以使用catchError错误处理，
可以使用retry重试失败请求

命名约定 美元符结束
 */